@model System.Collections.Generic.List<Coffe.Models.History>

@{
    ViewBag.Title = "History";
    Layout = "_Layout";
}

@section Styles
{
    <style>
        img{
            height: 100px;
            width: 100px;
            object-fit: cover;
        }
        td{
            text-align: center;
            vertical-align: middle !important;
        }
    </style>
}

<div class="col-md-12">
    <div class="card">
        <div class="card-header">
            <strong class="card-title">Tariximiz</strong>
        </div>
        <div class="card-body">
            <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
                <thead>
                <tr>
                    <th>Tariximiz</th>
                </tr>
                </thead>
                <tbody>
                @foreach (var history in Model)
                {
                    <tr>
                        <td>@history.Description</td>
                        <td>
                            <a asp-action="edit" asp-route-id="@history.Id" class="btn-warning btn">Dəyiş</a>
                        </td>
                    </tr>
                }
                </tbody>
            </table>
        </div>
    </div>
</div>
